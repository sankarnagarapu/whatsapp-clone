import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.sankarxace.whatsapp',
  appName: 'whatsapp-clone',
  webDir: 'www/browser'
};

export default config;
