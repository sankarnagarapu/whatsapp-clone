import { AllowOverflowDirective } from './allow-overflow.directive';

describe('AllowOverflowDirective', () => {
  it('should create an instance', () => {
    const directive = new AllowOverflowDirective();
    expect(directive).toBeTruthy();
  });
});
